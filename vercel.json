{
  "version": 2,
  "builds": [
    {
      "src": "backends/api-gateway/index.ts",
      "use": "@vercel/node",
      "config": {
        "maxDuration": 300
      }
    }
  ],
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/backends/api-gateway/index.ts"
    }
  ]
}
